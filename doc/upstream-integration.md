# 上游 2026.3.1 集成记录

## 固定基线

- 共同基点：`f8a57d344da9a5f938499683b2f277c4a444be28`。
- fork：`e079b0a807770e5a3297881e500522a7679fc45a`，`main`。
- 上游：`c3ed325f21e6c2573c67ff842d2ca5a6789f2e3d`，`V2026.3.1`。
- 核查日期：2026-09-22。双方分别有 63 / 53 个独有提交，合并预演有 65 个冲突路径。

## 不变量

- 保留现有 fork 功能、旧数据库和历史完整备份。
- fork 的 Room v4 与上游 v4 不同，保留 fork v1–v9，不能覆盖历史 schema。
- 本轮不支持官方另一套 v4 数据库直接升级；使用已有导入导出路径迁移官方用户数据。
- AI 配图保持展示用途；普通上传附件遵循多模态能力与预算。
- 数据库、完整备份、聊天归档分别版本化，不依赖历史备份中固定为 5 的数据库版本判断格式。

## 验收批次

- 基线：修复文档入口，建立 CI 和历史 schema 完整性检查。
- 合并：数据库、媒体、Repository、Prompt、生成、界面、构建依次解决冲突。
- 稳定性：恢复协调、前台服务计数、Provider 总并发、日志脱敏、外貌缓存、TTS 生命周期。
- 重构：仅拆分可验证的业务职责，不替换框架或改变已有交互语义。

## 后续需求

- 上游 #12：自动备份在恢复可靠性验收后单独实现。
- 上游 #9：本轮保留选择复制，分享和系统文本处理另行补齐。
- 上游 #4：保留单聊角色绑定；服务商与模型分离单独迁移。
- 上游 #3：验证已有导入导出，不重复开发。
- 桌面端、MCP 和群聊异构模型路由不在本轮范围内。

## 当前交付边界

- 集成分支为 `integrate/upstream-2026.3.1`，尚未合入 `main`，也未创建正式发布。
- `79be660` 是实际的双亲合并提交，父提交为 fork 稳定性批次 `aa135d7` 与上游 `c3ed325`；不是仅保存了一份补丁。
- 以下“已实现”描述代码状态，不等于完整验收通过。验证结果必须与提交对应，不用合并前的通过结果替代合并后的验证。

## 已实现的稳定性边界

- 前台服务租约获取失败不泄漏计数；图片任务的获取/释放失败不再跳过任务归属清理。
- 正文、摘要和图片提词共享 Provider 总并发额度；系统前台超时会取消对应任务。
- Logcat 与文件使用同一份脱敏结果，异常消息不再通过 Throwable 重载绕过出口。
- 角色外貌缓存写回验证完整输入快照；TTS 租约覆盖合成、排队与播放，清理保护在用音频。
- 单聊和群聊的整条复制遵循思维块设置；备份数据库版本取自 Room 版本常量。
- JVM 回归和 API 26/35 设备测试加入 CI；翻译缺口使用精确 Lint 基线，债务范围见构建文档，不算作已解决。

## 已实现的恢复一致性边界

- 增加应用级任务准入和数据代次，覆盖普通 Intent、排队任务、页面后台任务与应用级生成。
- 完整恢复具有持久回滚日志、启动恢复、同步偏好提交和失败关闭的恢复页面。
- 增加旧 v1 行字段默认值适配，修复 Gson 绕过 Kotlin 默认参数导致的旧备份兼容问题。
- 新增恢复失败回滚、模拟进程中断、文件恢复、损坏日志拒绝业务入口、取消时资源移交清理测试。

## 上游合并候选：待完整构建与设备验收

- 已将保存的冲突处理结果与后续稳定性提交进行三方合并，不回退恢复屏障、并发额度或日志隐私修复。
- v10 在 fork v9 上新增用量、模型能力及统一附件关系，使用独立 9→10 迁移，不复用上游 v4 历史。旧配图转成展示附件；旧列只保留用于兼容转换，正常写入为空。
- 完整备份写 v2、继续读 v1；v2 携带附件用途与用量表。聊天归档图片扩展写 v2，hash 去重不合并不同附件用途。
- 上游分页、多模态、配色与用量入口进入合并候选；摘要保留 fork 已测试的连续前缀选择算法，同时接入上游快照冲突检查。
- Room 已生成并提交 schema v10，历史 v1–v9 保持原样。合并候选已通过 Debug 应用和设备测试 APK 编译，但尚未通过完整 JVM、Release/R8 与设备验收，不用于发布。


## 设置职责与兼容回归补充

- 保留 fork 的首页、Prompt 行为、摘要、语音和备份分层，不重复引入上游首页内嵌的导入/参数编辑流程。
- 上游图片目录授权与缺图计数接入现有备份页；导入结果使用真实会话 ID，暂存所有权在取消或页面销毁时释放。
- Prompt 历史窗口上限放到 Prompt 行为页，并纳入备份偏好；非法数字仅保留为草稿，不误写成无限制。
- 语音配置摘要由 ViewModel 构建，不再由首页 Compose 直接读取偏好；仅展示配图的变化不使摘要输入快照失效。
- 补充所有 fork 历史 schema 到 v10、含旧配图及缺失字段的真实 V1 恢复、V2 附件恢复和 hash 去重下独立用途测试。
- CI 优先编译应用与设备测试，并提前上传编译报告；设备任务也保留构建日志，避免测试 APK 编译失败却没有可诊断报告。

## 最近一轮远程验证

运行 `35713315137` 对应提交 `9d6f093`，不是本轮修复后的结果：

| 验证项目 | 实际结果 |
| --- | --- |
| Debug 应用及 Android 测试 APK | 编译通过 |
| Lint | 通过；272 个既有资源键仍在精确翻译基线内 |
| JVM 测试 | 测试源码编译失败，没有执行用例 |
| Debug API 26 | 执行 135 项，8 项失败 |
| Debug API 35 | 执行 136 项，6 项失败 |
| R8 verification API 26 / 35 | APK 已构建；运行器缺少 `androidx.tracing.Trace`，实际执行 0 项 |

- Debug 设备报告中的 6 项完整备份往返/恢复故障测试全部通过，包括 V1/V2 恢复；这不代表其余迁移和归档测试通过。
- 合并前 `aa135d7` 的 366 项 JVM 通过仅是历史基线，不能计入当前合并候选的验收。

## 本轮收敛修复

- 9→10 迁移删除已经被三列索引覆盖的旧群聊索引；本地 SQLite 校验同时核对完整索引集合和唯一性，避免“新索引存在但旧索引残留”漏检。
- 生成最终接受内容为空时，允许清理已保存过流式草稿的新占位；继续校验会话、来源和附件，保护其他消息与图片引用。
- 单聊页面发布数据库加载结果时重新取图片协调器状态，避免覆盖选择器返回期间的实时草稿；API 26 的两处超时仍需设备复测确认。
- 归档测试适配图片扩展 V2，同时加强相同 hash 不同 `send_to_model` 关系的往返检查；保留 V1 默认行为及未来版本拒绝。
- 日志集成夹具明确开启并恢复父级开发者开关，不放松正式环境的隐私门控。
- 修复 JVM 测试缺失的归档夹具、导入、构造参数及摘要选择器旧接口；备份规则分别检查数据库域与暂存目录域，不再因预先过滤造成错误失败。
- 单聊与群聊的特殊模式全局提示开关进入 `PromptPreferences`，不再用未初始化 Android 偏好的异常伪装默认值；覆盖开关切换和所有生成模式。
- verification 单独保留测试运行器依赖的 tracing 入口；反射发现所需的测试 APK 规则与正式应用 R8 规则分开，不关闭应用混淆，也不以零用例作为成功。

## 本轮验证与下一步

- 本地历史 schema、SQLite 迁移夹具、Kotlin 语法及 `git diff --check` 通过。
- 使用缓存目录中的 JDK、Kotlin 编译器和正式依赖，实际执行归档编解码、图片载荷、备份隐私规则、备份格式化共 4 个 JVM 测试类，26 项通过；这是选定源码的独立编译执行，不是 Gradle 全套或 Android 设备验收。
- 仍需当前提交的全量 JVM、Lint、unsigned Release，以及 API 26/35 Debug 与 R8 设备复测。设备用例没有重新执行前，不把上述修复写成验收通过。
- 开发分支自动 CI 已在 `76f4688` 关闭。保留现有触发边界，集中验收另行安排，不偷偷恢复自动推送触发，也不通过修改 `main` 绕过。
- 先完成本轮兼容和运行器验证，再处理应用级生成仍持有 ViewModel 闭包的任务快照拆分；不在未稳定的合并之上扩大重构。
